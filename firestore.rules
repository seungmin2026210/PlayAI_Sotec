rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // 백엔드는 Firebase Admin SDK(서비스 계정)로만 접근하며, Admin SDK는 이 규칙을
      // 우회한다. 클라이언트에서의 직접 접근은 설계상 없으므로 명시적으로 막아둔다.
      allow read, write: if false;
    }
  }
}
